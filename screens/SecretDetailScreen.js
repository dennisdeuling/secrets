import React from 'react';
import { View, Text, SafeAreaView } from 'react-native';

const SecretDetailScreen = ({ navigation }) => {
	return (
		<SafeAreaView>
			<View>
				<Text>SecretDetailScreen</Text>
			</View>
		</SafeAreaView>
	);
};

export default SecretDetailScreen;
